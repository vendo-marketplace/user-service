spring:
  data:
    redis:
      host: localhost
      port: 6379
    mongodb:
      uri: mongodb://${MONGO_USERNAME:root}:${MONGO_PASSWORD:secret}@${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DATABASE:mydatabase}?authSource=admin&authMechanism=SCRAM-SHA-256
      authentication-database: admin

security:
  jwt:
    secret-key: secretkeysecretkeysecretkeysecretkeysecretkeysecretkeysecretkeysecretkeysecretkey
    access-expiration-time: 60000
    refresh-expiration-time: 120000

eureka:
  client:
    enabled: false

redis:
  password-recovery:
    email:
      prefix: "test-email-prefix:"
      ttl: 600
    otp:
      prefix: "test-otp-prefix:"
      ttl: 600
    attempts:
      prefix: "test-attempts-prefix:"
      ttl: 3600
  email-verification:
    email:
      prefix: "test-email-prefix:"
      ttl: 600
    otp:
      prefix: "test-otp-prefix:"
      ttl: 600
    attempts:
      prefix: "test-attempts-prefix:"
      ttl: 3600

kafka:
  host: localhost
  port: 9092
  events:
    email-otp-notification-event:
      topic: "EMAIL_OTP_NOTIFICATION_TEST_TOPIC"
      groupId: "email_otp_notification_test_group"
      container-factory: "consumerContainerFactory"
      properties:
        auto-offset-reset: "earliest"